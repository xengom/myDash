# myDash 환경 변수 설정 예시
# 이 파일을 .env로 복사하고 값을 수정하세요: cp .env.example .env

# ==========================================
# 데이터베이스 설정
# ==========================================
DATABASE_PATH=./data/mydash.db

# ==========================================
# OpenWeather API 설정 (선택사항)
# ==========================================
# API 키 발급: https://openweathermap.org/api
# 무료 플랜: 60 calls/minute, 1,000,000 calls/month
OPENWEATHER_API_KEY=your_api_key_here
WEATHER_CITY=Seoul
OPENWEATHER_UNITS=metric

# ==========================================
# Google OAuth 설정 (선택사항)
# ==========================================
# Google Cloud Console: https://console.cloud.google.com/
# 1. 프로젝트 생성
# 2. Calendar API, Gmail API, Tasks API 활성화
# 3. OAuth 2.0 클라이언트 ID 생성 (데스크톱 앱)
# 4. credentials.json 다운로드
GOOGLE_CREDENTIALS_PATH=.credentials.json
GOOGLE_TOKEN_PATH=.token.json

# ==========================================
# 새로고침 간격 (초)
# ==========================================
REFRESH_INTERVAL_SYSTEM=5        # 시스템 정보 (CPU, 메모리)
REFRESH_INTERVAL_WEATHER=1800    # 날씨 (30분)
REFRESH_INTERVAL_CALENDAR=900    # Google Calendar (15분)
REFRESH_INTERVAL_GMAIL=300       # Gmail (5분)
REFRESH_INTERVAL_TASKS=600       # Google Tasks (10분)
REFRESH_INTERVAL_STOCKS=60       # 주식 가격 (1분)

# ==========================================
# 로깅 설정
# ==========================================
LOG_LEVEL=INFO
LOG_FILE=./data/mydash.log

# ==========================================
# 캐시 설정
# ==========================================
CACHE_ENABLED=true
CACHE_TTL=3600                   # 캐시 유지 시간 (초)
